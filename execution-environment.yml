---
# AAP 2.6 custom EE — build from a pinned supported base image.

version: 3

dependencies:
  galaxy: collections/requirements.yml

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-26/ee-supported-rhel9:latest
